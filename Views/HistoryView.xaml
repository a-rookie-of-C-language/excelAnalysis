<UserControl x:Class="excel.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    <Grid Background="#FAFAFA" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="历史记录" FontSize="18" FontWeight="Bold"/>

        <DataGrid Grid.Row="2" x:Name="HistoryGrid"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  Background="#FFFFFF"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="200"/>
                <DataGridTextColumn Header="路径" Binding="{Binding FilePath}" Width="*"/>
                <DataGridTextColumn Header="分表" Binding="{Binding TableName}" Width="240"/>
                <DataGridTextColumn Header="导入时间" Binding="{Binding ImportedAt}" Width="200"/>
                <DataGridTextColumn Header="记录数" Binding="{Binding StudentCount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
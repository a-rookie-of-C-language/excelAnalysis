<UserControl x:Class="excel.Views.ReportGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    <Grid Background="#FAFAFA" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="报告生成" FontSize="18" FontWeight="Bold"/>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16"
                xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="报告类型" VerticalAlignment="Center" Width="80"/>
                <ComboBox Width="200" Margin="8,0,0,0" materialDesign:HintAssist.Hint="选择报告类型">
                    <ComboBoxItem Content="班级成绩汇总"/>
                    <ComboBoxItem Content="学生个人报告"/>
                    <ComboBoxItem Content="课程成绩分析"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="时间范围" VerticalAlignment="Center" Width="80"/>
                <DatePicker Width="160" Margin="8,0,0,0" materialDesign:HintAssist.Hint="开始日期"/>
                <TextBlock Text="至" VerticalAlignment="Center" Margin="8,0,8,0"/>
                <DatePicker Width="160" materialDesign:HintAssist.Hint="结束日期"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <CheckBox Content="包含图表" IsChecked="True"/>
                <CheckBox Content="包含明细" IsChecked="True" Margin="16,0,0,0"/>
                <CheckBox Content="导出为PDF" Margin="16,0,0,0"/>
            </StackPanel>

            <Button Grid.Row="6" Content="生成报告" Width="120" Height="36" HorizontalAlignment="Left"/>

            <StackPanel Grid.Row="8">
                <TextBlock Text="生成日志" FontWeight="Bold" Margin="0,0,0,8"/>
                <Border Background="#F5F5F5" CornerRadius="6" Padding="8" Height="160">
                    <TextBlock Text="—" Foreground="#666"/>
                </Border>
            </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
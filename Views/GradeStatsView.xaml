<UserControl x:Class="excel.Views.GradeStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    <Grid Background="#FAFAFA" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="成绩统计" FontSize="18" FontWeight="Bold"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <ComboBox Width="180" Margin="0,0,8,0" materialDesign:HintAssist.Hint="选择课程">
                <ComboBoxItem Content="语文"/>
                <ComboBoxItem Content="数学"/>
                <ComboBoxItem Content="英语"/>
                <ComboBoxItem Content="物理"/>
                <ComboBoxItem Content="化学"/>
            </ComboBox>
            <ComboBox Width="140" Margin="0,0,8,0" materialDesign:HintAssist.Hint="选择学期">
                <ComboBoxItem Content="上学期"/>
                <ComboBoxItem Content="下学期"/>
            </ComboBox>
            <Button Content="应用" Width="80"/>
        </StackPanel>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16"
                    materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="图表占位" FontWeight="Bold" Margin="0,0,0,8"/>
                    <Border Background="#F5F5F5" Height="220" CornerRadius="6"/>
                    <TextBlock Text="可在此集成柱状图/折线图等组件" Foreground="#666" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <UniformGrid Grid.Column="2" Columns="2">
                <Border Background="#FFFFFF" Padding="12" Margin="8" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1"
                        materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="最高分" FontWeight="Bold"/>
                        <TextBlock Text="—" FontSize="22" Foreground="#4CAF50"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" Padding="12" Margin="8" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1"
                        materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="最低分" FontWeight="Bold"/>
                        <TextBlock Text="—" FontSize="22" Foreground="#F44336"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" Padding="12" Margin="8" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="平均分" FontWeight="Bold"/>
                        <TextBlock Text="—" FontSize="22" Foreground="#3F51B5"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFFFF" Padding="12" Margin="8" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="及格率" FontWeight="Bold"/>
                        <TextBlock Text="—" FontSize="22" Foreground="#009688"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>
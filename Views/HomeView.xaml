<UserControl x:Class="excel.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    <Grid Background="#FAFAFA" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部：上传图标 + 文本标题 -->
        <DockPanel>
            <materialDesign:PackIcon Kind="Upload" Width="20" Height="20" Foreground="#616161" Margin="0,0,8,0"/>
            <TextBlock Text="文件导入" Style="{StaticResource TitleText}"/>
        </DockPanel>

        <!-- 上传区域：虚线边框，居中图标与说明文字 -->
        <Grid Grid.Row="2" Margin="0" x:Name="UploadArea" AllowDrop="True"
             >
            <Rectangle x:Name="DropHintBorder" Stroke="#D0D0D0"
                       Fill="Transparent" 
                       StrokeThickness="2" 
                       RadiusX="8" RadiusY="8"
                       StrokeDashArray="5 3"/>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Viewbox Width="72" Height="72" Stretch="Uniform">
                    <svgc:SvgViewbox IsHitTestVisible="False" 
                                     Source="pack://application:,,,/svg/upload.svg"/>
                </Viewbox>

                <TextBlock Text="支持 xlsx、xls、csv格式文件" FontSize="16" Margin="0,12,0,6"/>
                <TextBlock Text="文件大小不超过 10MB，支持拖拽上传、点击上传、粘贴上传" Foreground="#757575"/>

                <Button x:Name="SelectExcelButton" Content="选择Excel文件" Width="160" Margin="0,20,0,0"
                        Click="SelectExcelButton_OnClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
<Window x:Class="excel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:excel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:excel.ViewModels"
        mc:Ignorable="d"
        Title="学生成绩分析系统" Height="600" Width="1000" MinWidth="900" MinHeight="540"
        Style="{StaticResource MaterialDesignWindow}">
    <Window.DataContext>
        <viewModels:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 顶部栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#4A6FA5">
            <DockPanel Margin="12,0" VerticalAlignment="Center">
                <TextBlock Text="学生成绩分析系统" Foreground="White" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text=" | " Foreground="#B0BEC5" Margin="8,0"/>
                <TextBlock Text="{Binding SelectedItem.Name}" Foreground="#B0BEC5"/>
            </DockPanel>
        </Border>

        <!-- 左侧导航 -->
        <Border Grid.Row="1" Grid.Column="0" Background="#2C3E50"/>
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0" Background="#FFFFFF">
            <TextBlock Text="导航" Foreground="#000000" FontWeight="Bold" Margin="16,16,16,8"/>
            <ListBox ItemsSource="{Binding NavigationItems}" 
                     SelectedItem="{Binding SelectedItem}" 
                     Background="#FFFFFF" BorderThickness="0" Foreground="#ECEFF1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" 
                                   Padding="16" 
                                   Foreground="#000000"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <!-- 右侧内容 -->
        <Border Grid.Row="1" Grid.Column="1" Margin="16" Background="#FFFFFF" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <ContentControl Content="{Binding SelectedItem.View}"/>
        </Border>
    </Grid>
</Window>